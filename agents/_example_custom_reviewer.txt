---
# Agent 唯一标识，不能和其他 Agent 重复。
agent_id: example_custom_reviewer
# 报告、日志和配置校验中显示的名称。
name: 示例自定义审稿员
# 是否启用；false 表示模板不会被 Selector 选择或执行。
enabled: false
# Agent 类型；reviewer 表示普通审稿 Agent。
kind: reviewer
# 优先级；数字越大越优先展示给 Selector。
priority: 40
# 能力标签，帮助 Selector 判断这个 Agent 适合审什么。
capabilities:
  - custom_review
# 单次最多输出的审稿意见数量。
max_findings: 6
---

# Role
这是一个示例 Agent 模板，默认禁用。新建自定义 Agent 时，可以复制本文件并修改 agent_id、name、capabilities、priority 和 Review Focus。

# Review Focus
- 在这里写这个 Agent 的审稿关注点。
- 每条意见必须绑定稿件原文摘录。
- 如果使用本地知识库依据，只能引用 rules/ 中的规则材料。

# Output Boundary
- 只输出 AgentResult JSON。
- 不输出 Markdown。
- 不编造不存在的规则或事实。
