# 13 分阶段开发任务清单

本文件直接给 AI 编程体执行。每个阶段完成后必须运行测试或命令验证。

## 阶段 0：创建项目骨架

### 任务

```text
1. 创建 ai_review_tool 项目目录。
2. 创建 src/review_tool 包。
3. 创建 agents/cases/rules/style_guides/risky_phrases/examples/input/output 文件夹。
4. 创建 pyproject.toml。
5. 创建 .env.example。
6. 创建 README.md。
```

### 验收

```bash
python -m pip install -e .
review-tool --help
```

## 阶段 1：实现 Schema

### 文件

```text
src/review_tool/schemas.py
```

### 任务

实现：

```text
ArticleInput
ArticleSegment
ModelConfig
AppliesTo
AgentConfig
KnowledgeDocument
KnowledgeChunk
SelectorResult
AgentSelection
EvidenceRef
Finding
AgentResult
ReviewRunResult
ReviewSettings
```

### 验收

```text
pytest tests/test_schemas.py
```

## 阶段 2：实现 txt front matter 解析

### 文件

```text
src/review_tool/loaders/frontmatter.py
src/review_tool/loaders/text_file_loader.py
```

### 任务

实现：

```python
def parse_frontmatter_text(text: str) -> tuple[dict, str]: ...
def load_txt_with_frontmatter(path: Path) -> tuple[dict, str]: ...
```

### 验收

```text
能解析带 YAML front matter 的 txt。
能解析无 front matter 的 txt。
YAML 失败时给出明确错误。
```

## 阶段 3：实现 Agent 加载

### 文件

```text
src/review_tool/loaders/agent_loader.py
src/review_tool/agents/registry.py
```

### 任务

实现：

```text
load_agent_configs
AgentRegistry
config validation
catalog generation
```

### 验收

```bash
review-tool validate-config
review-tool show-agents
```

## 阶段 4：实现稿件加载与预处理

### 文件

```text
src/review_tool/review/preprocess.py
```

### 任务

实现：

```text
load_article_file
extract title
extract images
split segments
```

### 验收

```text
输入 sample_article.md，输出 ArticleInput 和 segments。
```

## 阶段 5：实现知识库加载与检索

### 文件

```text
src/review_tool/loaders/corpus_loader.py
src/review_tool/knowledge/chunker.py
src/review_tool/knowledge/retriever.py
```

### 任务

实现：

```text
load_corpus
chunk_documents
KnowledgeRetriever.retrieve
filter_context_for_agent
```

### 验收

```bash
review-tool index
```

## 阶段 6：实现 LLM 抽象与 Mock

### 文件

```text
src/review_tool/llm/base.py
src/review_tool/llm/mock_client.py
src/review_tool/llm/openai_client.py
src/review_tool/llm/structured.py
src/review_tool/llm/json_repair.py
```

### 任务

实现：

```text
LLMClient 抽象
MockLLMClient
OpenAIClient
call_with_schema
extract_json_text
```

### 验收

```text
Mock 模式下所有测试不调用真实 API。
```

## 阶段 7：实现 Selector Agent

### 文件

```text
src/review_tool/agents/selector.py
src/review_tool/review/routing.py
```

### 任务

实现：

```text
build_deterministic_hints
run_selector
validate_selector_result
fallback_select_agents
finalize_selected_agents
```

### 验收

```bash
review-tool select-agents input/article.md
```

## 阶段 8：实现通用 AgentRunner

### 文件

```text
src/review_tool/agents/runner.py
src/review_tool/agents/prompt_builder.py
```

### 任务

实现：

```text
build_reviewer_prompt
run_reviewer
输出补全 agent_id/agent_name/finding_id
错误转 AgentResult
```

### 验收

```text
Mock 模式下 risk_reviewer 能返回 Finding。
```

## 阶段 9：实现多 Agent 执行器

### 文件

```text
src/review_tool/review/executor.py
```

### 任务

实现：

```text
asyncio 并行执行
并发限制
超时处理
失败降级
关键 Agent 失败 warning
```

### 验收

```text
一个 Agent 故意失败，整体审稿仍生成报告。
```

## 阶段 10：实现合并、评分、仲裁

### 文件

```text
src/review_tool/review/merger.py
src/review_tool/review/scoring.py
src/review_tool/review/arbitrator.py
```

### 任务

实现：

```text
collect_findings
merge_findings
score_finding
assign_bucket
build_arbitration_result
```

### 验收

```text
重复 Finding 被合并。
高风险规则依据进入 must_fix。
低置信度建议进入 reference。
```

## 阶段 11：实现报告生成

### 文件

```text
src/review_tool/review/report_builder.py
src/review_tool/exporters/json_exporter.py
src/review_tool/exporters/markdown_exporter.py
```

### 任务

实现：

```text
build_final_report
export_json
export_markdown
```

### 验收

```text
output/{run_id}/report.md 存在且包含四个 bucket。
```

## 阶段 12：实现 ReviewEngine

### 文件

```text
src/review_tool/engine.py
```

### 任务

串联所有模块。

### 验收

```bash
review-tool review input/article.md --debug
```

应该生成：

```text
article_snapshot.md
selector_result.json
retrieved_context.json
agent_results.json
report.json
report.md
debug/
```

## 阶段 13：完善 CLI

### 文件

```text
src/review_tool/cli.py
```

### 命令

```text
review
select-agents
validate-config
index
show-agents
```

### 验收

所有命令可运行。

## 阶段 14：写样例 txt

### 文件

创建：

```text
agents/*.txt
cases/*.txt
rules/*.txt
style_guides/*.txt
risky_phrases/*.txt
examples/*.txt
input/article.md
```

### 验收

不需要真实 prompt 完美，但配置必须可解析，Mock 模式能跑。

## 阶段 15：端到端验收

### 命令

```bash
review-tool validate-config
review-tool show-agents
review-tool index
review-tool select-agents input/article.md
review-tool review input/article.md
pytest
```

### 成功标准

```text
全部命令成功。
生成报告。
测试通过。
无未捕获异常。
```
